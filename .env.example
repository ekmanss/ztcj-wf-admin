# API
DATABASE_URL=mysql://user:password@localhost:3306/rootdata
PORT=3001
CORS_ORIGIN=http://localhost:5173

# Admin
VITE_API_BASE_URL=http://localhost:3001/api
VITE_CLERK_PUBLISHABLE_KEY=

# Optional: only works in Vite dev mode. Set true to enter protected pages
# with a local preview session while designing UI without the API auth flow.
VITE_DEV_AUTH_ENABLED=false
